Notes: just run "soure .bashrc" to get all these environment variables to work

Installing Java version for Windows (Oracle JDK)
After unpacking the zip file in your desired location, configure it to add to PATH variable:

    export JAVA_HOME="/C/tools/jdk-11.0.25"
    export PATH="${JAVA_HOME}/bin:${PATH}"


Verify it works: 
    which java 
    java --version

--------------------------------------------------------

hADOOP BINARIES
Note: we use this "hadoop-2.7.2" due to errors in writing parquets when using 3.0 above
```
$ HADOOP_VERSION="2.7.2"
PREFIX="https://raw.githubusercontent.com/cdarlint/winutils/master/hadoop-${HADOOP_VERSION}/bin/"


FILES="hadoop.dll hadoop.exp hadoop.lib hadoop.pdb libwinutils.lib winutils.exe winutils.pdb"

for FILE in ${FILES}; do
  curl -o "${FILE}" "${PREFIX}/${FILE}";
done
```

--Option_2
PREFIX_2="https://github.com/cdarlint/winutils/tree/master/hadoop-2.7.2/bin"
for FILE in ${FILES}; do
  curl -o "${FILE}" "${PREFIX_2}/${FILE}";
done
```

Note: add a file if you want to specify directory of download i.e. >>>>  curl -o "file_path_name" 



Add to path:
Example below where we create a variable with the file path/file_name and MAP it to the Env Variable PATH
    export HADOOP_HOME="/C/tools/hadoop-2.7.2"  
    export PATH="${HADOOP_HOME}/bin:${PATH}"


-----------------------------------------------------------------
INSTALLING Spark
Note: we use "spark-3.3.2-bin-hadoop2.tgz" due to errors in writing parquets when using 3.0 above
Select version 3.3.2 and unpack it in a folder
Or download link using >>> $ curl -O https://archive.apache.org/dist/spark/spark-3.3.2/spark-3.3.2-bin-hadoop2.tgz
Extract file in path using >>>  tar xzfv spark-3.3.2-bin-hadoop2.tgz 

    export SPARK_HOME="/C/tools/spark-3.3.2-bin-hadoop2"
    export PATH="${SPARK_HOME}/bin:${PATH}"

Option 2 (if working in github repo). Full relative path or full path
    export SPARK_HOME="./spark-3.3.2-bin-hadoop2/"
    export SPARK_HOME="/c/Users/lourh/OneDrive/Documentos/Junior/Data Engineerig Zoomcamp 2025/data-engineering-zoomcamp/05-batch/spark/spark-3.3.2-bin-hadoop3"
    export PATH="${SPARK_HOME}/bin:${PATH}"


--------------------------------------------------------
Testing Spark
Go to the directory specified 
(I had to place the file in the github repo due to the env variables linked to the command we will run after i.e. $JAVA_HOME)
    cd spark-3.3.2-bin-hadoop2

Run spark-shell
    ./bin/spark-shell.cmd
--------------------------------------------------------
Note:
You can add C:\tools\jdk-11.0.25 files in 05-batch of your git repo and map to these files here if easier

--------------------------------------------------------
bashrc
To avoid logging the variables required to run the batch examples everytimem you log into the computer.
The .bashrc file is a script that runs whenever a new terminal session is started in interactive mode.
    Put these to bashrc >> run "nano .bashrc" to edit the file 
    source .bashrc >> to rerun and re-evaluate the code in bashrc to your environment

----------------------------------------------------------
PySpark
To run PySpark, we first need to add it to PYTHONPATH:
    export PYTHONPATH="${SPARK_HOME}/python/:$PYTHONPATH"
    export PYTHONPATH="${SPARK_HOME}/python/lib/py4j-0.10.9.5-src.zip:$PYTHONPATH"

On windows you may have to do path conversion from Unix-style to windows style:

    SPARK_WIN=`cygpath -w ${SPARK_HOME}`

    export PYTHONPATH="${SPARK_WIN}\\python\\"
    export PYTHONPATH="${SPARK_WIN}\\python\\lib\\py4j-0.10.9.5-src.zip;$PYTHONPATH"
